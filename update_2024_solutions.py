import sqlite3

conn = sqlite3.connect('D:/source/999.claude_test/exam_app/exam.db')
cur = conn.cursor()

solutions = {}

# ID 184: IPv6 주소
solutions[184] = """## IPv6 주소

### (1) 주소 2001:1:1:1::1이 통합(Aggregatable) 글로벌 유니캐스트 주소인 이유 (4점)

**통합 글로벌 유니캐스트 주소(Aggregatable Global Unicast Address)**는 IPv6에서 전 세계적으로 라우팅 가능한 주소를 의미하며, 다음과 같은 이유로 해당 주소가 이에 해당한다.

- **주소 범위**: 통합 글로벌 유니캐스트 주소는 **2000::/3** 범위에 속하는 주소이다. 즉, 첫 3비트가 "001"로 시작하는 주소(2000:: ~ 3FFF::)가 이에 해당한다.
- **해당 주소 분석**: 2001:1:1:1::1의 첫 번째 16비트는 2001(16진수)이며, 이진수로 변환하면 **0010 0000 0000 0001**이다. 상위 3비트가 **001**이므로 2000::/3 범위에 포함된다.
- **계층적 구조**: 이 주소는 **TLA(Top-Level Aggregation) ID**, **NLA(Next-Level Aggregation) ID**, **SLA(Site-Level Aggregation) ID**, **인터페이스 ID**로 구성되어 계층적 라우팅 집약(aggregation)이 가능하다.
- 따라서 2001:1:1:1::1은 글로벌하게 유일하며 라우팅 가능한 통합 글로벌 유니캐스트 주소이다.

### (2) IPv6 주소 산출 및 설명 (6점)

**주어진 조건:**
- 기관 할당 주소 블록: **2001:1:1::/48**
- 서브넷 ID: **3**
- 인터페이스 ID: **1:1:1:1**

**IPv6 주소 구조 (128비트):**

| 구분 | 글로벌 라우팅 프리픽스 | 서브넷 ID | 인터페이스 ID |
|------|-------------------|---------|------------|
| 비트 수 | 48비트 | 16비트 | 64비트 |

**주소 산출 과정:**

1. **글로벌 라우팅 프리픽스 (48비트)**: 2001:0001:0001 (2001:1:1::/48에서 상위 48비트)
2. **서브넷 ID (16비트)**: 0003 (서브넷 ID가 3이므로 16비트로 표현하면 0003)
3. **인터페이스 ID (64비트)**: 0001:0001:0001:0001

**최종 IPv6 주소: 2001:1:1:3:1:1:1:1**

**이유:**
- /48 프리픽스는 상위 48비트(처음 3개의 16비트 그룹)를 고정하므로 2001:0001:0001까지가 글로벌 라우팅 프리픽스이다.
- 49~64비트(4번째 16비트 그룹)는 서브넷 ID로 사용되며, 서브넷 ID 3은 0003으로 표현된다.
- 65~128비트(나머지 4개의 16비트 그룹)는 인터페이스 ID로 1:1:1:1이 할당된다.
- 이를 조합하면 **2001:0001:0001:0003:0001:0001:0001:0001**, 축약하면 **2001:1:1:3:1:1:1:1**이 된다.
"""

# ID 185: 터널링 프로토콜
solutions[185] = """## 터널링(Tunneling) 프로토콜

### (1) 캡슐화(Encapsulating)의 의미와 기능 (7점)

#### 캡슐화의 의미

**캡슐화(Encapsulation)**란 하나의 네트워크 프로토콜의 패킷(데이터 단위)을 다른 프로토콜의 페이로드(Payload) 부분에 포함시켜 전송하는 기술을 의미한다. 원래의 패킷을 새로운 프로토콜의 헤더로 감싸는(wrapping) 과정이다.

#### 캡슐화의 기능

- **프로토콜 호환성 제공**: 서로 다른 프로토콜 간의 통신을 가능하게 한다. 예를 들어, IPv6 패킷을 IPv4 네트워크를 통해 전송할 때 IPv6 패킷을 IPv4 패킷 안에 캡슐화한다.
- **데이터 보호 및 보안**: 원본 패킷을 외부 헤더로 감싸므로 내부 데이터의 구조가 중간 네트워크에 노출되지 않아 **보안성**이 향상된다.
- **네트워크 확장**: 서로 다른 네트워크 토폴로지나 주소 체계를 사용하는 네트워크 간에 **가상 터널(Virtual Tunnel)**을 형성하여 논리적으로 연결할 수 있다.
- **투명한 전송**: 중간 네트워크 장비들은 외부 헤더만 처리하고 내부의 원본 패킷은 투명하게 전달되므로, 중간 장비의 수정 없이도 새로운 프로토콜을 사용할 수 있다.
- **VPN(가상사설망) 구현**: 캡슐화를 통해 사설 네트워크의 트래픽을 공용 네트워크를 통해 안전하게 전달할 수 있으며, 이는 VPN의 핵심 기술이다.
- **QoS 지원**: 외부 헤더에 별도의 QoS 정보를 설정하여 터널 구간에서의 서비스 품질을 관리할 수 있다.

### (2) 터널링 프로토콜 종류 3가지 (3점)

1. **PPTP (Point-to-Point Tunneling Protocol)**: Microsoft에서 개발한 2계층 터널링 프로토콜로, PPP 프레임을 IP 패킷으로 캡슐화하여 VPN 연결을 제공한다.

2. **L2TP (Layer 2 Tunneling Protocol)**: Cisco의 L2F와 Microsoft의 PPTP를 결합한 2계층 터널링 프로토콜로, IPSec과 함께 사용되어 보안성을 강화한다.

3. **IPSec (IP Security)**: 3계층(네트워크 계층)에서 동작하는 터널링 프로토콜로, AH(Authentication Header)와 ESP(Encapsulating Security Payload)를 통해 인증, 무결성, 기밀성을 제공한다.
"""

# ID 186: 멀티캐스팅
solutions[186] = """## 멀티캐스팅(Multicasting)

### (1) 멀티캐스팅 IP주소에서 그룹(Group) ID가 정의되는 이유 (4점)

**멀티캐스트 IP 주소**에서 **그룹 ID**가 정의되는 이유는 다음과 같다.

- **효율적인 그룹 통신**: 멀티캐스팅은 하나의 송신자가 특정 그룹에 속한 다수의 수신자에게 동시에 데이터를 전송하는 방식이다. 그룹 ID를 통해 **동일한 데이터를 수신하고자 하는 호스트들을 논리적으로 묶어** 식별할 수 있다.
- **네트워크 대역폭 절약**: 그룹 ID로 수신 대상을 특정함으로써, 유니캐스트처럼 개별 전송하거나 브로드캐스트처럼 모든 호스트에 전송하는 것이 아니라, **관심 있는 그룹의 호스트에게만** 패킷을 전달하여 네트워크 자원을 효율적으로 사용할 수 있다.
- **동적 그룹 관리**: 호스트는 그룹 ID를 기반으로 특정 멀티캐스트 그룹에 **동적으로 가입(Join)하거나 탈퇴(Leave)**할 수 있어, 유연한 그룹 관리가 가능하다.
- **라우터의 효율적 포워딩**: 라우터는 그룹 ID를 기반으로 멀티캐스트 라우팅 테이블을 구성하여, 해당 그룹의 수신자가 존재하는 인터페이스로만 패킷을 전달할 수 있다.

### (2) IGMP 프로토콜의 그룹 관리 기능 (6점)

**IGMP(Internet Group Management Protocol)**는 호스트와 로컬 멀티캐스트 라우터 간에 멀티캐스트 그룹 멤버십을 관리하는 프로토콜이다.

#### 주요 그룹 관리 기능

**1. 그룹 가입(Joining a Group)**
- 호스트가 특정 멀티캐스트 그룹에 가입하고자 할 때, **멤버십 보고(Membership Report)** 메시지를 라우터에게 전송한다.
- 라우터는 해당 그룹의 멤버가 자신의 네트워크에 존재함을 인식하고, 멀티캐스트 트래픽을 해당 인터페이스로 전달한다.

**2. 그룹 멤버십 조회(Querying)**
- 멀티캐스트 라우터는 주기적으로 **멤버십 질의(Membership Query)** 메시지를 전송하여, 현재 네트워크에 특정 그룹의 멤버가 존재하는지 확인한다.
- **일반 질의(General Query)**: 모든 그룹에 대해 멤버십을 확인한다.
- **특정 그룹 질의(Group-Specific Query)**: 특정 그룹에 대한 멤버십만 확인한다.

**3. 멤버십 응답(Reporting)**
- 질의를 수신한 호스트 중 해당 그룹의 멤버인 호스트는 **멤버십 보고(Membership Report)** 메시지로 응답한다.
- **응답 억제(Report Suppression)**: 동일 그룹의 다른 호스트가 이미 보고한 경우, 중복 보고를 억제하여 네트워크 트래픽을 줄인다 (IGMPv1, v2).

**4. 그룹 탈퇴(Leaving a Group)**
- 호스트가 그룹을 탈퇴할 때 **탈퇴(Leave Group)** 메시지를 라우터에 전송한다 (IGMPv2 이상).
- 라우터는 **특정 그룹 질의**를 보내어 해당 그룹의 다른 멤버가 있는지 확인한 후, 멤버가 없으면 해당 그룹의 트래픽 전달을 중단한다.

**5. 타이머 관리**
- 라우터는 각 그룹에 대한 **멤버십 타이머**를 유지하며, 일정 시간 내에 멤버십 보고가 없으면 해당 그룹의 멤버가 없는 것으로 판단하고 그룹을 삭제한다.
"""

# ID 187: 디스크 스케줄링
solutions[187] = """## 디스크 스케줄링 알고리즘 (30점)

**주어진 조건:**
- 실린더 수: 100개 (0~100)
- 현재 헤드 위치: 실린더 30
- 대기 큐: 79, 68, 11, 74, 10, 89, 65, 87, 26, 15 (요청 순서)
- FCFS 총 탐색 거리: 441개 실린더 (참고용)

### (1) SSTF, SCAN, LOOK 알고리즘의 처리 순서 (12점)

#### SSTF (Shortest Seek Time First)

SSTF는 현재 헤드 위치에서 **가장 가까운 요청을 먼저 처리**하는 알고리즘이다.

| 단계 | 현재 위치 | 선택 | 이동 거리 |
|------|---------|------|---------|
| 1 | 30 | 26 | 4 |
| 2 | 26 | 15 | 11 |
| 3 | 15 | 11 | 4 |
| 4 | 11 | 10 | 1 |
| 5 | 10 | 65 | 55 |
| 6 | 65 | 68 | 3 |
| 7 | 68 | 74 | 6 |
| 8 | 74 | 79 | 5 |
| 9 | 79 | 87 | 8 |
| 10 | 87 | 89 | 2 |

**처리 순서: 30 -> 26 -> 15 -> 11 -> 10 -> 65 -> 68 -> 74 -> 79 -> 87 -> 89**

#### SCAN (엘리베이터 알고리즘)

SCAN은 헤드가 한 방향으로 이동하면서 요청을 처리하고, **끝에 도달하면 반대 방향**으로 이동한다. (현재 헤드가 증가 방향으로 이동 중이라 가정)

**증가 방향 이동 (30 -> 100):**
30 -> 65 -> 68 -> 74 -> 79 -> 87 -> 89 -> (100 끝 도달)

**감소 방향 이동 (100 -> 0):**
-> 26 -> 15 -> 11 -> 10

**처리 순서: 30 -> 65 -> 68 -> 74 -> 79 -> 87 -> 89 -> (100) -> 26 -> 15 -> 11 -> 10**

#### LOOK

LOOK은 SCAN과 유사하지만, **실제 요청이 있는 마지막 위치까지만 이동**하고 반대 방향으로 전환한다.

**증가 방향 이동:**
30 -> 65 -> 68 -> 74 -> 79 -> 87 -> 89 (요청이 있는 마지막 위치에서 반전)

**감소 방향 이동:**
-> 26 -> 15 -> 11 -> 10

**처리 순서: 30 -> 65 -> 68 -> 74 -> 79 -> 87 -> 89 -> 26 -> 15 -> 11 -> 10**

### (2) SSTF, SCAN, LOOK의 총 탐색 거리 계산 (12점)

#### SSTF 총 탐색 거리

|30-26| + |26-15| + |15-11| + |11-10| + |10-65| + |65-68| + |68-74| + |74-79| + |79-87| + |87-89|
= 4 + 11 + 4 + 1 + 55 + 3 + 6 + 5 + 8 + 2 = **99개 실린더**

#### SCAN 총 탐색 거리

|30-65| + |65-68| + |68-74| + |74-79| + |79-87| + |87-89| + |89-100| + |100-26| + |26-15| + |15-11| + |11-10|
= 35 + 3 + 6 + 5 + 8 + 2 + 11 + 74 + 11 + 4 + 1 = **160개 실린더**

또는 간단히 계산: (100 - 30) + (100 - 10) = 70 + 90 = **160개 실린더**

#### LOOK 총 탐색 거리

|30-65| + |65-68| + |68-74| + |74-79| + |79-87| + |87-89| + |89-26| + |26-15| + |15-11| + |11-10|
= 35 + 3 + 6 + 5 + 8 + 2 + 63 + 11 + 4 + 1 = **138개 실린더**

또는 간단히 계산: (89 - 30) + (89 - 10) = 59 + 79 = **138개 실린더**

### (3) SSTF, SCAN과 비교한 LOOK 스케줄링의 장점 (6점)

#### SSTF 대비 LOOK의 장점

- **기아(Starvation) 문제 해결**: SSTF는 헤드 근처의 요청을 우선 처리하므로, 헤드에서 먼 위치의 요청은 무한히 대기하는 **기아 현상**이 발생할 수 있다. LOOK은 한 방향으로 순차적으로 처리하므로 모든 요청이 공정하게 처리된다.
- **응답 시간의 예측 가능성**: LOOK은 방향성이 있는 이동 패턴을 따르므로 요청의 응답 시간을 보다 예측 가능하게 만든다.

#### SCAN 대비 LOOK의 장점

- **불필요한 이동 제거**: SCAN은 디스크의 끝(실린더 0 또는 최대 실린더)까지 이동해야 하지만, LOOK은 **실제 요청이 존재하는 마지막 실린더까지만** 이동한 후 방향을 전환한다. 이로 인해 불필요한 탐색 거리가 줄어든다.
- **탐색 효율성 향상**: 위 예시에서 SCAN은 160개 실린더, LOOK은 138개 실린더로, LOOK이 **22개 실린더만큼 적은 이동 거리**를 보여 더 효율적이다.
- **처리량(Throughput) 향상**: 불필요한 이동이 없어 같은 시간 내에 더 많은 요청을 처리할 수 있어 전체적인 디스크 성능이 향상된다.
"""

# ID 188: 조인 (문제 2)
solutions[188] = """## 조인(Join)에 관한 답변 (30점)

### (1) 외부 조인의 목적과 정의 (6점)

**외부 조인(Outer Join)의 목적:**
- 일반적인 내부 조인(Inner Join)은 두 릴레이션에서 조인 조건을 만족하는 튜플만 결과에 포함시키므로, 조건을 만족하지 않는 튜플은 결과에서 **누락**된다.
- 외부 조인의 목적은 조인 조건을 만족하지 않는 튜플도 결과에 포함시켜 **정보의 손실을 방지**하는 것이다.

**외부 조인의 정의:**
- **외부 조인(Outer Join)**이란 두 릴레이션을 조인할 때, 조인 조건을 만족하는 튜플뿐만 아니라 **조건을 만족하지 않는 튜플도 결과 릴레이션에 포함**시키는 조인 연산이다.
- 대응되는 튜플이 없는 속성 값은 **NULL**로 채운다.

### (2) 왼쪽 외부 조인, 오른쪽 외부 조인, 완전 외부 조인 연산 설명 (12점)

**왼쪽 외부 조인(Left Outer Join):**
- 왼쪽 릴레이션의 **모든 튜플**을 결과에 포함시킨다.
- 오른쪽 릴레이션에서 조인 조건을 만족하는 튜플이 있으면 결합하고, 없으면 오른쪽 릴레이션의 속성 값을 **NULL**로 채운다.

**오른쪽 외부 조인(Right Outer Join):**
- 오른쪽 릴레이션의 **모든 튜플**을 결과에 포함시킨다.
- 왼쪽 릴레이션에서 조인 조건을 만족하는 튜플이 있으면 결합하고, 없으면 왼쪽 릴레이션의 속성 값을 **NULL**로 채운다.

**완전 외부 조인(Full Outer Join):**
- 양쪽 릴레이션의 **모든 튜플**을 결과에 포함시킨다.
- 왼쪽과 오른쪽 모두에서 조인 조건을 만족하지 않는 튜플도 포함하며, 대응되는 값이 없는 속성은 **NULL**로 채운다.
- 왼쪽 외부 조인과 오른쪽 외부 조인의 합집합과 동일하다.

### (3) 릴레이션 R과 S의 조인 결과 (12점)

**주어진 릴레이션:**

R(A, B, C): {(a1, b1, c1), (a2, b2, c2)}
S(C, D, E): {(c1, d1, e1), (c3, d2, e2)}

공통 속성: **C** (자연 조인 기준)

**자연 조인 (Natural Join):**

| A | B | C | D | E |
|---|---|---|---|---|
| a1 | b1 | c1 | d1 | e1 |

- R의 (a1, b1, c1)과 S의 (c1, d1, e1)이 C=c1로 일치하여 결합

**왼쪽 외부 조인 (Left Outer Join):**

| A | B | C | D | E |
|---|---|---|---|---|
| a1 | b1 | c1 | d1 | e1 |
| a2 | b2 | c2 | NULL | NULL |

**오른쪽 외부 조인 (Right Outer Join):**

| A | B | C | D | E |
|---|---|---|---|---|
| a1 | b1 | c1 | d1 | e1 |
| NULL | NULL | c3 | d2 | e2 |

**완전 외부 조인 (Full Outer Join):**

| A | B | C | D | E |
|---|---|---|---|---|
| a1 | b1 | c1 | d1 | e1 |
| a2 | b2 | c2 | NULL | NULL |
| NULL | NULL | c3 | d2 | e2 |
"""

# ID 189: 페이지 교체 (문제 3)
solutions[189] = """## 페이지 교체 알고리즘 (10점)

**주어진 조건:**
- 메모리 프레임: 3개 (비어있음)
- 페이지 참조열: 0, 1, 2, 2, 4, 2, 0, 2, 2, 6, 100, 2, 5, 0, 100

### (1) FIFO 알고리즘의 페이지 부재 발생 페이지 번호 (5점)

FIFO는 **가장 먼저 적재된 페이지를 먼저 교체**하는 알고리즘이다.

| 참조 | 프레임1 | 프레임2 | 프레임3 | 부재 여부 |
|-----|--------|--------|--------|---------|
| 0 | **0** | - | - | O (부재) |
| 1 | 0 | **1** | - | O (부재) |
| 2 | 0 | 1 | **2** | O (부재) |
| 2 | 0 | 1 | 2 | X (적중) |
| 4 | **4** | 1 | 2 | O (부재, 0 교체) |
| 2 | 4 | 1 | 2 | X (적중) |
| 0 | 4 | **0** | 2 | O (부재, 1 교체) |
| 2 | 4 | 0 | 2 | X (적중) |
| 2 | 4 | 0 | 2 | X (적중) |
| 6 | 4 | 0 | **6** | O (부재, 2 교체) |
| 100 | **100** | 0 | 6 | O (부재, 4 교체) |
| 2 | 100 | **2** | 6 | O (부재, 0 교체) |
| 5 | 100 | 2 | **5** | O (부재, 6 교체) |
| 0 | **0** | 2 | 5 | O (부재, 100 교체) |
| 100 | 0 | **100** | 5 | O (부재, 2 교체) |

**페이지 부재가 발생한 페이지 번호 순서: 0, 1, 2, 4, 0, 6, 100, 2, 5, 0, 100**

총 **11회** 페이지 부재 발생

### (2) LRU 알고리즘의 페이지 부재 발생 페이지 번호 (5점)

LRU는 **가장 오래 사용되지 않은 페이지를 교체**하는 알고리즘이다.

| 참조 | 프레임1 | 프레임2 | 프레임3 | 부재 여부 | 비고 |
|-----|--------|--------|--------|---------|------|
| 0 | **0** | - | - | O (부재) | |
| 1 | 0 | **1** | - | O (부재) | |
| 2 | 0 | 1 | **2** | O (부재) | |
| 2 | 0 | 1 | 2 | X (적중) | |
| 4 | **4** | 1 | 2 | O (부재) | LRU=0 교체 |
| 2 | 4 | 1 | 2 | X (적중) | |
| 0 | 4 | **0** | 2 | O (부재) | LRU=1 교체 |
| 2 | 4 | 0 | 2 | X (적중) | |
| 2 | 4 | 0 | 2 | X (적중) | |
| 6 | **6** | 0 | 2 | O (부재) | LRU=4 교체 |
| 100 | 6 | **100** | 2 | O (부재) | LRU=0 교체 |
| 2 | 6 | 100 | 2 | X (적중) | |
| 5 | **5** | 100 | 2 | O (부재) | LRU=6 교체 |
| 0 | 5 | **0** | 2 | O (부재) | LRU=100 교체 |
| 100 | **100** | 0 | 2 | O (부재) | LRU=5 교체 |

**페이지 부재가 발생한 페이지 번호 순서: 0, 1, 2, 4, 0, 6, 100, 5, 0, 100**

총 **10회** 페이지 부재 발생
"""

# ID 190: 정규화 (문제 4)
solutions[190] = """## 정규화 (10점)

**주어진 조건:**
- 수강 릴레이션 (제3정규형 만족)
- 기본 키: (학번, 과목)
- 속성: 학번, 과목, 강사

| 학번 | 과목 | 강사 |
|------|------|------|
| 23002 | 소프트웨어공학 | 박혜자 |
| 23002 | 병렬처리 | 김영태 |
| 24036 | 인공지능 | 강태원 |
| 24036 | 데이터베이스 | 이한나 |
| 23048 | 소프트웨어공학 | 박혜자 |

### (1) 수강 릴레이션에 존재하는 3가지 갱신 이상과 발생 이유 (5점)

#### 갱신 이상의 종류

**1. 삽입 이상(Insertion Anomaly)**
- 새로운 강사가 특정 과목을 담당하게 되었으나, 아직 수강하는 학생이 없는 경우 해당 정보를 삽입할 수 없다. 기본 키의 일부인 학번이 NULL이 될 수 없기 때문이다.
- 예: "김철수" 강사가 "운영체제" 과목을 담당하지만 수강 학생이 없으면 삽입 불가

**2. 삭제 이상(Deletion Anomaly)**
- 특정 학생의 수강 정보를 삭제하면, 해당 과목의 강사 정보도 함께 삭제될 수 있다.
- 예: 학번 24036의 "인공지능" 수강을 삭제하면 "강태원" 강사가 "인공지능"을 담당한다는 정보도 소실

**3. 수정 이상(Modification Anomaly)**
- 특정 과목의 강사를 변경할 때, 해당 과목을 수강하는 모든 튜플을 수정해야 한다. 일부만 수정하면 데이터 불일치가 발생한다.
- 예: "소프트웨어공학"의 강사를 변경하면 학번 23002와 23048의 두 튜플 모두 수정해야 함

#### 갱신 이상의 발생 이유

- **함수 종속성**: 수강 릴레이션에서 {과목} -> {강사}라는 함수 종속이 존재한다. 즉, 과목이 결정되면 강사가 결정된다.
- 이 함수 종속에서 결정자인 "과목"은 기본 키의 일부이지만 후보 키가 아니다. 이는 **BCNF를 위반**하는 것으로, 모든 결정자가 후보 키가 아니기 때문에 갱신 이상이 발생한다.

### (2) 수강 릴레이션을 BCNF로 정규화 (5점)

#### BCNF 위반 분석

- 함수 종속: {학번, 과목} -> {강사}, {과목} -> {강사}
- {과목} -> {강사}에서 결정자 "과목"이 후보 키가 아니므로 BCNF를 위반한다.

#### BCNF 정규화 수행

BCNF 위반 함수 종속 {과목} -> {강사}를 기준으로 릴레이션을 분해한다.

**릴레이션 1: 수강2 (학번, 과목)**
- 기본 키: (학번, 과목)

| 학번 | 과목 |
|------|------|
| 23002 | 소프트웨어공학 |
| 23002 | 병렬처리 |
| 24036 | 인공지능 |
| 24036 | 데이터베이스 |
| 23048 | 소프트웨어공학 |

**릴레이션 2: 과목강사 (과목, 강사)**
- 기본 키: 과목

| 과목 | 강사 |
|------|------|
| 소프트웨어공학 | 박혜자 |
| 병렬처리 | 김영태 |
| 인공지능 | 강태원 |
| 데이터베이스 | 이한나 |

- 분해된 두 릴레이션 모두 BCNF를 만족한다.
- 무손실 조인 분해이며, "과목" 속성을 통해 원래 릴레이션을 복원할 수 있다.
"""

# ID 191: 교착 상태 (문제 5)
solutions[191] = """## 교착 상태(Deadlock) (10점)

### (1) 자원 할당 그래프에서 교착상태에 걸린 스레드 (3점)

주어진 자원 할당 그래프를 분석하면:

**자원 현황:**
- Lock1: 인스턴스 1개 (T1에 할당됨)
- Lock2: 인스턴스 1개 (T3에 할당됨)
- 프린터: 인스턴스 2개 (T4에 할당됨)
- 파일: 인스턴스 1개 (T5에 할당됨)

**요청 관계 분석:**
- T1: Lock1을 보유, Lock2를 요청
- T2: 프린터를 요청, Lock2를 요청
- T3: Lock2를 보유, Lock1을 요청
- T4: 프린터를 보유 (다른 요청 없음)
- T5: 파일을 보유 (다른 요청 없음)

**교착 상태 사이클:**
- T1 -> Lock2 -> T3 -> Lock1 -> T1 (순환 대기 발생)

**교착상태에 걸린 스레드: T1, T3**

### (2) 교착 상태 발생의 4가지 필요충분조건 (7점)

교착 상태가 발생하려면 다음 **4가지 조건이 동시에** 성립해야 한다 (Coffman 조건).

**1. 상호 배제(Mutual Exclusion)**
- 자원은 한 번에 하나의 프로세스(스레드)만 사용할 수 있어야 한다.
- 하나의 프로세스가 자원을 사용 중이면 다른 프로세스는 해당 자원을 사용할 수 없다.

**2. 점유 대기(Hold and Wait)**
- 프로세스가 최소 하나의 자원을 점유(보유)한 상태에서 다른 프로세스가 점유하고 있는 추가 자원을 요청하며 대기해야 한다.
- 자원을 보유하면서 동시에 다른 자원을 기다리는 상태이다.

**3. 비선점(No Preemption)**
- 이미 할당된 자원은 프로세스가 자발적으로 해제할 때까지 강제로 빼앗을 수 없다.
- 다른 프로세스가 사용 중인 자원을 강제로 회수할 수 없는 조건이다.

**4. 순환 대기(Circular Wait)**
- 프로세스들 사이에 자원 요청의 **순환적 체인**이 존재해야 한다.
- P1 -> P2 -> P3 -> ... -> Pn -> P1과 같이, 각 프로세스가 다음 프로세스가 보유한 자원을 요청하는 순환 구조가 형성되어야 한다.

이 4가지 조건은 **필요충분조건**으로, 하나라도 성립하지 않으면 교착 상태는 발생하지 않는다. 따라서 교착 상태를 예방하려면 이 중 하나 이상의 조건이 성립하지 않도록 해야 한다.
"""

# ID 192: SQL 쿼리 (문제 6)
solutions[192] = """## SQL 쿼리 작성 (10점)

**주어진 사원 릴레이션:**

| 사번 | 이름 | 직급 | 관리자 | 급여 | 부서번호 |
|------|------|------|--------|------|---------|
| 2106 | 김영태 | 대리 | 1003 | 2500000 | 2 |
| 3426 | 박혜자 | 과장 | 4377 | 3000000 | 1 |
| 3011 | 이한나 | 부장 | 4377 | 4000000 | 3 |
| 1003 | 강태원 | 과장 | 4377 | 3000000 | 2 |
| 3427 | 최상일 | 사원 | 3011 | 1500000 | 3 |
| 1365 | 김상경 | 사원 | 3426 | 1500000 | 1 |
| 4377 | 이형원 | 사장 | NULL | 5000000 | 2 |

(사번은 기본 키, 급여는 원 단위, NULL 값은 ^로 표시)

### (1) 이씨 성을 가진 사원들의 이름, 직급, 부서번호 검색 SQL문 (5점)

```sql
SELECT 이름, 직급, 부서번호
FROM 사원
WHERE 이름 LIKE '이%';
```

**설명:**
- **LIKE '이%'** 조건을 사용하여 이름이 "이"로 시작하는(이씨 성을 가진) 사원을 검색한다.
- **%**는 와일드카드로 "이" 뒤에 어떤 문자열이 와도 일치함을 의미한다.

**실행 결과:**

| 이름 | 직급 | 부서번호 |
|------|------|---------|
| 이한나 | 부장 | 3 |
| 이형원 | 사장 | 2 |

### (2) 급여가 3000000원 이상이고 4500000원 이하인 사원들의 이름, 직급, 급여 검색 SQL문 (5점)

```sql
SELECT 이름, 직급, 급여
FROM 사원
WHERE 급여 >= 3000000 AND 급여 <= 4500000;
```

또는 **BETWEEN** 연산자를 사용하여:

```sql
SELECT 이름, 직급, 급여
FROM 사원
WHERE 급여 BETWEEN 3000000 AND 4500000;
```

**설명:**
- **BETWEEN A AND B**는 급여가 A 이상 B 이하인 조건을 의미한다.

**실행 결과:**

| 이름 | 직급 | 급여 |
|------|------|------|
| 박혜자 | 과장 | 3000000 |
| 이한나 | 부장 | 4000000 |
| 강태원 | 과장 | 3000000 |
"""

# ID 194: 클래스 설계 원칙 (SOLID)
solutions[194] = """## 클래스(Class) 설계의 원칙 (SOLID 원칙) (30점)

### (1) 단일책임 원칙 (SRP: Single Responsibility Principle) (6점)

**단일책임 원칙**이란 하나의 클래스는 **단 하나의 책임(기능)**만을 가져야 한다는 원칙이다.

- 클래스를 변경해야 하는 이유는 오직 하나뿐이어야 한다.
- 하나의 클래스에 여러 책임이 혼재되면, 하나의 책임을 수정할 때 다른 책임에 영향을 줄 수 있어 **결합도가 높아지고 유지보수가 어려워진다.**
- 예를 들어, 사원 클래스가 급여 계산과 데이터베이스 저장을 모두 담당하면 SRP를 위반한 것이다. 급여 계산 클래스와 데이터 저장 클래스로 분리해야 한다.
- SRP를 준수하면 클래스의 **응집도(Cohesion)**가 높아지고, 변경의 영향 범위가 최소화된다.

### (2) 개방폐쇄 원칙 (OCP: Open-Closed Principle) (6점)

**개방폐쇄 원칙**이란 소프트웨어 구성 요소(클래스, 모듈, 함수)는 **확장에는 열려(Open) 있고, 수정에는 닫혀(Closed) 있어야** 한다는 원칙이다.

- 기존 코드를 변경하지 않으면서도 새로운 기능을 추가할 수 있어야 한다.
- **추상화(Abstraction)**와 **다형성(Polymorphism)**을 활용하여 구현한다.
- 인터페이스나 추상 클래스를 정의하고, 새로운 기능은 이를 구현하는 새로운 클래스를 추가하는 방식으로 확장한다.
- 예를 들어, 도형의 넓이를 계산하는 기능에서 새로운 도형이 추가되더라도 기존 코드를 수정하지 않고 새로운 도형 클래스를 추가하기만 하면 된다.

### (3) 리스코프 교체 원칙 (LSP: Liskov Substitution Principle) (6점)

**리스코프 교체 원칙**이란 **하위 클래스(자식 클래스)**는 언제나 **상위 클래스(부모 클래스)**를 대체할 수 있어야 한다는 원칙이다.

- 프로그램에서 부모 클래스의 인스턴스 대신 자식 클래스의 인스턴스를 사용해도 프로그램의 **정확성(correctness)**이 보장되어야 한다.
- 자식 클래스는 부모 클래스의 **사전 조건(precondition)**을 강화하거나 **사후 조건(postcondition)**을 약화시키면 안 된다.
- 상속 관계에서 "IS-A" 관계가 의미론적으로도 성립해야 한다.
- 예를 들어, 직사각형 클래스를 상속받은 정사각형 클래스가 직사각형의 가로/세로를 독립적으로 설정하는 동작을 보장하지 못하면 LSP를 위반한 것이다.

### (4) 의존관계 역전 원칙 (DIP: Dependency Inversion Principle) (6점)

**의존관계 역전 원칙**이란 고수준 모듈은 저수준 모듈에 의존하면 안 되며, 둘 다 **추상화에 의존**해야 한다는 원칙이다.

- **고수준 모듈(비즈니스 로직)**이 **저수준 모듈(세부 구현)**에 직접 의존하지 않고, 둘 사이에 **추상화(인터페이스)** 계층을 두어 의존 방향을 역전시킨다.
- 추상화는 세부 사항에 의존하면 안 되며, 세부 사항이 추상화에 의존해야 한다.
- 예를 들어, 주문 처리 모듈이 특정 데이터베이스(MySQL)에 직접 의존하는 대신, 데이터 접근 인터페이스에 의존하도록 설계하면, 데이터베이스를 교체해도 주문 처리 모듈을 수정할 필요가 없다.
- DIP를 적용하면 **모듈 간 결합도가 낮아지고**, 테스트 용이성과 유연성이 향상된다.

### (5) 인터페이스 분리 원칙 (ISP: Interface Segregation Principle) (6점)

**인터페이스 분리 원칙**이란 클라이언트는 자신이 사용하지 않는 메서드에 의존하도록 강제되어서는 안 된다는 원칙이다.

- 하나의 범용적인 인터페이스보다는 **여러 개의 구체적인 인터페이스**로 분리하는 것이 바람직하다.
- 거대한 인터페이스(fat interface)를 작고 구체적인 인터페이스로 분리하여, 각 클라이언트가 필요한 인터페이스만 구현하도록 한다.
- 예를 들어, 복합기의 인터페이스를 프린터, 스캐너, 팩스 인터페이스로 분리하면, 프린터 기능만 필요한 클라이언트는 프린터 인터페이스만 의존하면 된다.
- ISP를 적용하면 인터페이스의 **응집도가 높아지고**, 불필요한 의존성을 제거하여 **변경의 영향 범위를 최소화**할 수 있다.
"""

# ID 195: MVC 스타일
solutions[195] = """## MVC 스타일 (10점)

### (1) MVC 스타일의 3가지 컴포넌트 (3점)

MVC 스타일의 기능을 담당하는 3가지 컴포넌트는 다음과 같다.

1. **모델(Model)**: 애플리케이션의 데이터와 비즈니스 로직을 담당하는 컴포넌트
2. **뷰(View)**: 사용자에게 데이터를 시각적으로 표현하는 컴포넌트 (UI)
3. **컨트롤러(Controller)**: 사용자의 입력을 처리하고 모델과 뷰를 연결하는 컴포넌트

### (2) MVC 스타일의 작동 원리 (3점)

MVC 스타일의 작동 원리는 다음과 같다.

1. **사용자 입력**: 사용자가 뷰(View)를 통해 입력을 하면, 해당 입력 이벤트가 **컨트롤러(Controller)**에 전달된다.
2. **비즈니스 로직 처리**: 컨트롤러는 사용자의 입력을 해석하여 적절한 **모델(Model)**의 메서드를 호출하고, 모델은 데이터를 처리하거나 비즈니스 로직을 수행한다.
3. **상태 변경 통지**: 모델의 상태(데이터)가 변경되면 모델은 자신에게 등록된 **뷰(View)**에게 상태 변경을 통지(Notify)한다.
4. **화면 갱신**: 뷰는 모델로부터 변경된 데이터를 가져와(pull) 화면을 **갱신(Update)**하여 사용자에게 최신 정보를 표시한다.

이처럼 **모델, 뷰, 컨트롤러**가 각각의 역할을 분리하여 수행하며, 상호작용을 통해 애플리케이션이 동작한다.

### (3) MVC 스타일의 장점 2가지 (4점)

**1. 독립적 개발 및 유지보수 용이성**
- 모델, 뷰, 컨트롤러가 서로 독립적으로 분리되어 있으므로, 각 컴포넌트를 **독립적으로 개발, 테스트, 수정**할 수 있다. 예를 들어, UI(뷰)를 변경해도 비즈니스 로직(모델)에 영향을 주지 않으며, 반대로 비즈니스 로직을 수정해도 UI를 변경할 필요가 없다.

**2. 다중 뷰 지원 및 재사용성**
- 하나의 모델에 대해 **여러 개의 뷰**를 동시에 제공할 수 있다. 예를 들어, 동일한 데이터를 테이블 형태, 그래프 형태, 차트 형태 등 다양한 방식으로 동시에 표현할 수 있다. 또한 모델이나 컨트롤러를 다른 애플리케이션에서 **재사용**할 수 있어 개발 생산성이 향상된다.
"""

# ID 196: 요구(requirements)
solutions[196] = """## 요구(Requirements) (10점)

### (1) 소프트웨어 요구의 정의 (4점)

**소프트웨어 요구(Software Requirements)**란 사용자가 특정 문제를 해결하거나 목표를 달성하기 위해 소프트웨어가 갖추어야 할 **조건이나 능력**을 말한다.

구체적으로, 소프트웨어 요구는 다음을 포함한다:
- 사용자가 필요로 하는 **기능, 성능, 제약 조건** 등을 명세화한 것
- 시스템이 만족시켜야 할 **서비스(service)**와 **제약사항(constraint)**의 기술
- 계약, 표준, 명세서 등 공식 문서에 의해 부과되는 조건이나 능력
- IEEE 정의: "사용자가 문제를 해결하거나 목표를 달성하기 위해 필요로 하는 조건 또는 능력으로서, 시스템이나 시스템 컴포넌트가 계약, 표준, 명세서 또는 기타 공식적으로 부과된 문서를 충족시키기 위해 갖추어야 할 조건이나 능력"

### (2) 기능적 요구와 비기능적 요구의 차이점 (6점)

#### 기능적 요구(Functional Requirements)

- 시스템이 **무엇을 해야 하는지(What)**를 정의하는 요구사항이다.
- 시스템이 제공해야 할 **서비스, 기능, 동작**을 명세한다.
- 입력에 대해 시스템이 어떻게 **반응**해야 하는지, 특정 상황에서 어떻게 **동작**해야 하는지를 기술한다.
- 예: "사용자는 회원가입을 할 수 있어야 한다", "시스템은 주문 내역을 조회하는 기능을 제공해야 한다"

#### 비기능적 요구(Non-Functional Requirements)

- 시스템이 기능을 **어떻게(How)** 수행해야 하는지에 대한 **품질 속성**을 정의하는 요구사항이다.
- 시스템의 **성능, 보안, 신뢰성, 사용성, 유지보수성** 등 품질 관련 제약 조건을 명세한다.
- 시스템 전체에 걸쳐 적용되는 제약사항이 많다.
- 예: "시스템은 동시 접속자 10,000명을 지원해야 한다(성능)", "응답 시간은 3초 이내여야 한다(성능)", "99.9%의 가용성을 보장해야 한다(신뢰성)"

#### 주요 차이점

| 구분 | 기능적 요구 | 비기능적 요구 |
|------|----------|-----------|
| **초점** | 시스템의 기능과 동작 | 시스템의 품질과 제약 |
| **관점** | 무엇을(What) 할 것인가 | 어떻게(How) 할 것인가 |
| **범위** | 개별 기능에 적용 | 시스템 전체에 적용 |
| **검증** | 기능 테스트로 검증 | 성능 측정, 품질 평가로 검증 |
| **예시** | 로그인, 검색, 결제 기능 | 응답시간, 보안, 확장성 |
"""

# ID 197: 소프트웨어 테스트
solutions[197] = """## 소프트웨어 테스트 (10점)

### (1) 단위 테스트(Unit Test)의 개념 (5점)

**단위 테스트(Unit Test)**란 소프트웨어의 **최소 단위인 개별 모듈(함수, 메서드, 클래스 등)**을 독립적으로 테스트하여 각 단위가 설계된 대로 올바르게 동작하는지 검증하는 테스트 기법이다.

- **테스트 대상**: 개별 함수, 메서드, 클래스 등 소프트웨어의 최소 구성 단위
- **목적**: 각 모듈이 독립적으로 정확하게 동작하는지 확인
- **수행 시점**: 코딩 완료 후 가장 먼저 수행되는 테스트 단계
- **수행 주체**: 주로 개발자(프로그래머)가 직접 수행
- **방법**: 모듈의 인터페이스, 자료 구조, 경계 조건, 독립적 경로, 오류 처리 경로 등을 테스트
- 외부 의존성을 제거하기 위해 **스텁(Stub)**이나 **드라이버(Driver)**를 사용하여 독립적인 환경에서 테스트한다.

### (2) 블랙박스 테스트(Blackbox Test)의 개념 (5점)

**블랙박스 테스트(Blackbox Test)**란 소프트웨어의 **내부 구조나 동작 원리를 알지 못하는 상태**에서, **입력값과 출력값**만을 기반으로 소프트웨어의 기능이 요구사항에 맞게 올바르게 동작하는지 검증하는 테스트 기법이다.

- **관점**: 소프트웨어를 내부가 보이지 않는 "블랙박스"로 간주하고, 외부에서 관찰 가능한 동작만으로 테스트
- **기반**: 소프트웨어의 **요구사항 명세서(specification)**를 기반으로 테스트 케이스를 설계
- **초점**: 소프트웨어가 "무엇을" 하는지(기능)에 초점, 내부 코드 구조는 고려하지 않음
- **주요 기법**:
  - **동치 분할(Equivalence Partitioning)**: 입력 데이터를 동등한 클래스로 분할하여 대표값으로 테스트
  - **경계값 분석(Boundary Value Analysis)**: 입력 범위의 경계값을 중심으로 테스트
  - **원인-결과 그래프(Cause-Effect Graph)**: 입력 조건과 출력 결과의 관계를 그래프로 표현하여 테스트
  - **오류 추정(Error Guessing)**: 경험과 직관을 바탕으로 오류가 발생할 가능성이 높은 부분을 테스트
"""

# ID 198: 소프트웨어공학 정의와 목표 (빈칸 채우기)
solutions[198] = """## 소프트웨어공학의 정의와 목표 (10점)

### 빈칸 채우기 답안

IEEE Computer Society에 의하면 소프트웨어 공학은 "소프트웨어의 개발, 운용, ( 1 ) 및 ( 2 )에 대한 체계적인 접근방법"으로 정의되어 있다. 소프트웨어공학의 목표는 ( 3 )좋은 소프트웨어를 ( 4 )비용으로 미리 계획된 ( 5 )에 맞추어 개발하는 것이다.

### 각 빈칸의 답

**(1) 유지보수(Maintenance)**

소프트웨어 생명주기에서 개발 이후의 단계로, 소프트웨어를 운용하면서 발생하는 오류 수정, 기능 개선, 환경 변화에 대한 적응 등의 활동을 포함한다.

**(2) 폐기(Retirement)**

소프트웨어가 더 이상 사용되지 않거나 새로운 시스템으로 교체될 때 기존 소프트웨어를 체계적으로 폐기하는 과정이다.

**(3) 품질(Quality)**

소프트웨어공학의 궁극적 목표는 사용자의 요구를 만족시키는 **품질 좋은** 소프트웨어를 개발하는 것이다. 품질에는 신뢰성, 효율성, 사용성, 유지보수성 등이 포함된다.

**(4) 최소(Minimum)**

경제적으로 효율적인 개발을 위해 **최소한의 비용**으로 소프트웨어를 개발해야 한다.

**(5) 일정(Schedule)**

프로젝트 관리 측면에서 미리 계획된 **일정(기간)** 내에 소프트웨어를 완성하는 것이 중요하다. 일정 지연은 비용 증가와 신뢰도 하락의 원인이 된다.

### 완성된 문장

> IEEE Computer Society에 의하면 소프트웨어 공학은 "소프트웨어의 개발, 운용, **유지보수** 및 **폐기**에 대한 체계적인 접근방법"으로 정의되어 있다. 소프트웨어공학의 목표는 **품질** 좋은 소프트웨어를 **최소** 비용으로 미리 계획된 **일정**에 맞추어 개발하는 것이다.
"""

for qid, sol in solutions.items():
    cur.execute('UPDATE questions SET solution = ? WHERE id = ?', (sol, qid))
    print(f'Updated ID {qid}')

conn.commit()
conn.close()
print('All solutions saved successfully!')
