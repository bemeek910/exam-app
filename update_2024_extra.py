import sqlite3

conn = sqlite3.connect('D:/source/999.claude_test/exam_app/exam.db')
cur = conn.cursor()

solutions = {}

# ID 182: 변조(Modulation)
solutions[182] = """## 변조(Modulation) (30점)

### (1) QPSK와 DQPSK 방식의 차이점 (8점)

#### QPSK (Quadrature Phase Shift Keying)

**QPSK**는 반송파의 위상을 4가지(0도, 90도, 180도, 270도)로 변화시켜 한 번에 **2비트**의 데이터를 전송하는 디지털 변조 방식이다.

- 입력 비트에 따라 **절대 위상값**을 할당한다 (예: 00->45도, 01->135도, 11->225도, 10->315도).
- 수신측에서는 수신된 신호의 **절대 위상**을 기준 반송파와 비교하여 데이터를 복조한다.
- **동기 검파(Coherent Detection)** 방식을 사용하며, 수신기에서 반송파의 정확한 위상 기준이 필요하다.

#### DQPSK (Differential Quadrature Phase Shift Keying)

**DQPSK**는 QPSK의 변형으로, 절대 위상이 아닌 **이전 심볼 대비 위상 변화량(차분 위상)**으로 데이터를 표현하는 방식이다.

- 입력 비트에 따라 이전 심볼 대비 **위상 변화량**을 할당한다 (예: 00->0도, 01->90도, 11->180도, 10->270도).
- 수신측에서는 연속된 두 심볼 간의 **위상 차이**를 이용하여 데이터를 복조한다.
- **비동기 검파(Non-Coherent Detection)**가 가능하여 기준 반송파의 정확한 위상 동기가 불필요하다.

#### 주요 차이점

| 구분 | QPSK | DQPSK |
|------|------|-------|
| **위상 할당** | 절대 위상 | 차분 위상 (이전 심볼 대비 변화량) |
| **복조 방식** | 동기 검파 (기준 반송파 필요) | 비동기 검파 가능 |
| **위상 기준** | 절대 위상 기준 필요 | 이전 심볼이 기준 |
| **구현 복잡도** | 복잡 (동기화 회로 필요) | 상대적으로 간단 |
| **성능** | BER 성능 우수 | QPSK 대비 약 2~3dB 성능 열화 |
| **위상 모호성** | 위상 모호성 문제 존재 | 위상 모호성 문제 해결 |

### (2) 신호 P(t)의 주파수 스펙트럼 (10점)

**주어진 조건:**
- 변조 신호(Modulating Signal): m(t) = A_m * cos(2*pi*f_m*t)
- 반송파 신호(Carrier Signal): c(t) = A_c * cos(2*pi*f_c*t)
- 곱셈기(Mixer)를 통해 P(t) = m(t) x c(t) 생성
- 조건: f_c > f_m

**P(t) 수식 유도:**

P(t) = m(t) x c(t) = A_m * cos(2*pi*f_m*t) x A_c * cos(2*pi*f_c*t)

삼각함수 곱의 공식 적용: cos(A) * cos(B) = 1/2 * [cos(A-B) + cos(A+B)]

P(t) = (A_m * A_c / 2) * [cos(2*pi*(f_c - f_m)*t) + cos(2*pi*(f_c + f_m)*t)]

**주파수 스펙트럼 설명:**

P(t)의 주파수 스펙트럼은 **두 개의 주파수 성분**으로 구성된다:

- **(f_c - f_m)** 위치에 진폭 **A_m * A_c / 2**의 스펙트럼 (하측 주파수, LSB)
- **(f_c + f_m)** 위치에 진폭 **A_m * A_c / 2**의 스펙트럼 (상측 주파수, USB)

**스펙트럼 형태:**
- x축(주파수)에서 (f_c - f_m)과 (f_c + f_m) 두 위치에 동일한 크기의 임펄스(스파이크)가 나타난다.
- 원래 반송파 주파수 f_c에는 성분이 존재하지 않는다 (억압 반송파, DSB-SC 방식).
- 이는 **DSB-SC(Double Sideband Suppressed Carrier)** 변조의 특성으로, 반송파 성분 없이 상측파대(USB)와 하측파대(LSB)만 존재한다.

**이유:**
- 시간 영역에서의 두 코사인 신호의 곱셈은 주파수 영역에서 **주파수 이동(Frequency Shifting)**에 해당한다.
- 기저대역 신호 f_m이 반송파 주파수 f_c를 중심으로 (f_c - f_m)과 (f_c + f_m)으로 이동한다.

### (3) OFDM에서 직교의 의미와 부반송파 간 주파수 조건 (12점)

#### 직교(Orthogonal)의 의미

OFDM에서 **직교(Orthogonal)**란 각 부반송파(Sub-carrier) 신호들이 서로 **수학적으로 직교(orthogonal)** 관계에 있어, 하나의 부반송파 신호를 다른 부반송파 신호와 곱하여 한 심볼 구간(T) 동안 적분하면 그 결과가 **0**이 되는 성질을 의미한다.

- 수학적 정의: 두 부반송파 f_i와 f_j에 대해
  - 적분(0~T) [cos(2*pi*f_i*t) * cos(2*pi*f_j*t)] dt = 0 (i != j)
- 직교성이 보장되면 각 부반송파의 스펙트럼이 서로 **중첩(overlap)**되더라도 수신측에서 **상호 간섭(ICI: Inter-Carrier Interference) 없이** 각 부반송파의 데이터를 독립적으로 복조할 수 있다.
- 이를 통해 기존 FDM 방식보다 **주파수 효율성**이 크게 향상된다. 기존 FDM은 부반송파 간 보호 대역(Guard Band)이 필요하지만, OFDM은 직교성 덕분에 부반송파 스펙트럼이 겹쳐도 간섭이 발생하지 않는다.

#### 직교성을 확보하기 위한 부반송파 간 주파수 조건

직교성을 확보하기 위해서는 인접 부반송파 간의 주파수 간격이 다음 조건을 만족해야 한다:

**부반송파 간 주파수 간격(Delta_f) = 1/T = 1/T_s**

여기서 T_s는 **OFDM 심볼 구간(Symbol Duration)**이다.

- 즉, 인접한 부반송파 간의 주파수 간격은 **심볼 구간의 역수**와 같아야 한다.
- N개의 부반송파를 사용하는 경우, k번째 부반송파의 주파수는 f_k = f_0 + k * (1/T)이다 (k = 0, 1, 2, ..., N-1).
- 이 조건을 만족하면 한 심볼 구간 동안 각 부반송파는 정수 배의 주기를 가지며, 서로 직교 관계가 성립한다.
- OFDM에서는 이 직교성을 효율적으로 구현하기 위해 **IFFT(역고속푸리에변환)**를 송신측에서, **FFT(고속푸리에변환)**를 수신측에서 사용한다.
"""

# ID 183: 신호
solutions[183] = """## 신호(Signal) (10점)

### (1) 지터(Jitter)의 의미와 최대 허용 지터 값 (7점)

#### 지터(Jitter)의 의미

**지터(Jitter)**란 디지털 비트열(Bitstream) 전송에서 신호의 타이밍(전이 시점)이 이상적인 위치로부터 **시간적으로 변동(deviation)**하는 현상을 의미한다.

- 이상적인 디지털 신호에서 각 비트의 전이(transition)는 정확한 시간 간격으로 발생해야 하지만, 실제로는 잡음, 전자기 간섭, 클럭 불안정 등의 원인으로 전이 시점이 앞뒤로 **흔들리는(fluctuation)** 현상이 발생한다.
- 지터는 신호의 **위상 변동**으로도 표현되며, 과도한 지터는 수신측에서 비트 판정 오류를 유발하여 **비트 오류율(BER)**을 증가시킨다.
- 지터의 단위는 **UI(Unit Interval)** 또는 시간 단위(초, ps, ns)로 표현한다.
  - **1 UI**는 한 비트 구간(bit period)의 시간 길이에 해당한다.

#### 최대 허용 지터 값 계산

**주어진 조건:**
- 전송률: 1 Mbps
- 지터 규격: 0.1 UI 이하

**풀이:**

1. **1 UI(Unit Interval) 계산:**
   - 전송률이 1 Mbps이므로 비트 주기(T) = 1 / (1 x 10^6) = 1 us (마이크로초) = 1,000 ns
   - 따라서 1 UI = 1 us = 1,000 ns

2. **최대 허용 지터 값:**
   - 지터 규격 = 0.1 UI
   - 최대 허용 지터 = 0.1 x 1 UI = 0.1 x 1 us = **0.1 us = 100 ns**

**답: 최대 허용 지터 값은 0.1 us (100 ns)이다.**

### (2) 전자기파 주파수가 높을수록 나타나는 현상 (3점)

전자기파 신호의 주파수가 높을수록 다음과 같은 현상이 나타난다.

**1. 파장(Wavelength) 측면:**
- 주파수와 파장은 반비례 관계(c = f x lambda)이므로, 주파수가 높을수록 **파장이 짧아진다.**

**2. 회절성(Diffraction) 측면:**
- 파장이 짧아지면 장애물을 돌아가는 **회절 능력이 감소**한다. 즉, 주파수가 높을수록 장애물 뒤쪽으로 신호가 전달되기 어려워진다.

**3. 직진성(Directivity) 측면:**
- 주파수가 높을수록 (파장이 짧을수록) 전자기파의 **직진성이 강해진다.** 빛에 가까운 성질을 가지게 되어 직선으로 전파하려는 특성이 커진다.
"""

for qid, sol in solutions.items():
    cur.execute('UPDATE questions SET solution = ? WHERE id = ?', (sol, qid))
    print(f'Updated ID {qid}')

conn.commit()
conn.close()
print('Extra solutions saved successfully!')
